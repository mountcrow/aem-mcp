# ─── AEM Instance URL ─────────────────────────────────────────────────────────
# Your AEM author instance base URL (no trailing slash)
# Local:        http://localhost:4502
# AEM Cloud:    https://author-pXXXXX-eYYYYYY.adobeaemcloud.com
AEM_BASE_URL=http://localhost:4502

# ─── Authentication ───────────────────────────────────────────────────────────
# Choose 'basic' for local/dev instances or 'token' for AEM as a Cloud Service
AEM_AUTH_TYPE=basic

# Basic auth (used when AEM_AUTH_TYPE=basic)
AEM_USERNAME=admin
AEM_PASSWORD=admin

# Token auth (used when AEM_AUTH_TYPE=token)
# Run `npm run get-token` to fetch a token automatically, or paste one manually.
AEM_ACCESS_TOKEN=

# ─── OAuth Server-to-Server Credentials (for auto token refresh) ──────────────
# From Adobe Developer Console → your project → OAuth Server-to-Server
# Required for `npm run get-token` and for automatic token refresh in the server
AEM_CLIENT_ID=
AEM_CLIENT_SECRET=

# IMS scopes — copy the EXACT value from:
#   Developer Console → your project → OAuth Server-to-Server → Credential details → Scopes
# Do NOT use generic values; the string must match what Console shows for your specific credential.
# Example: AdobeID,openid,aem.cloud,additional_info.projectedProductContext,...
AEM_SCOPES=
